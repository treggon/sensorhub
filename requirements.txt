
# Core web framework
fastapi>=0.115
uvicorn[standard]>=0.30

# Serialization & performance
pydantic>=2.7,<3
orjson>=3.9
uvloop>=0.19

# Concurrency & websockets
websockets>=13.0

# Sensors & IO
pyserial>=3.5

# UBX / NMEA / RTCM toolchain
# (Previous pins were tailored to Python 3.8; modernize for 3.10)
pyubx2>=1.2.48
pynmeagps>=1.0.43
pyrtcm>=1.1.2

msgpack>=1.0

# Numeric & vision
# Jetson (aarch64) has OpenCV provided by JetPack; pip wheels for opencv-python(-headless)
# are not shipped for aarch64. Use system cv2 on Jetson, pip wheel elsewhere.
numpy>=1.26,<2.0
opencv-python-headless>=4.9 ; platform_machine!="aarch64"

# Camera streaming / files
aiofiles>=23.1

# RPLidar (choose ONE and match your imports)
pyrplidar>=0.1.2
# rplidar>=0.9.2

# Imaging (fixes: ModuleNotFoundError: No module named 'PIL')
pillow>=10.0.0
